language: node_js
before_install:
  - 'nvm install-latest-npm'
script:
  - 'if [ -n "${COVERAGE-}" ]; then npm run coverage ; fi'
matrix:
  fast_finish: true
  include:
    - node_js: "4"
      env: COVERAGE=true
