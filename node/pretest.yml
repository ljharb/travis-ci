language: node_js
before_install:
  - 'nvm install-latest-npm'
script:
  - 'if [ -n "${PRETEST-}" ]; then npm run pretest ; fi'
matrix:
  fast_finish: true
  include:
    - node_js: "lts/*"
      env: PRETEST=true
